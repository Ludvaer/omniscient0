<p style="color: green"><%= notice %></p>

<%= render @account_activation %>

<div>
  <%= link_to "Edit this account activation", edit_account_activation_path(@account_activation) %> |
  <%= link_to "Back to account activations", account_activations_path %>

  <%= button_to "Destroy this account activation", @account_activation, method: :delete %>
</div>
