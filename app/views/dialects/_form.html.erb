<%= form_with(model: dialect) do |form| %>
  <% if dialect.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(dialect.errors.count, "error") %> prohibited this dialect from being saved:</h2>

      <ul>
        <% dialect.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :language_id, style: "display: block" %>
    <%= form.number_field :language_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
