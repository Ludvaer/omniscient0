<%= form_with(model: word) do |form| %>
  <% if word.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(word.errors.count, "error") %> prohibited this word from being saved:</h2>

      <ul>
        <% word.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :spelling, style: "display: block" %>
    <%= form.text_field :spelling %>
  </div>

  <div>
    <%= form.label :dialect_id, style: "display: block" %>
    <%= form.number_field :dialect_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
