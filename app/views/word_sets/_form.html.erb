<%= form_with(model: word_set) do |form| %>
  <% if word_set.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(word_set.errors.count, "error") %> prohibited this word_set from being saved:</h2>

      <ul>
        <% word_set.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
